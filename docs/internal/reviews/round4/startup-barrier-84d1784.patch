diff --git a/net/crates/net/sdk/src/a2a_journal.rs b/net/crates/net/sdk/src/a2a_journal.rs
index 254cd8384..cb6e23a86 100644
--- a/net/crates/net/sdk/src/a2a_journal.rs
+++ b/net/crates/net/sdk/src/a2a_journal.rs
@@ -2441,6 +2441,22 @@ impl A2aAdmissionJournal {
                 let _serialized = serialized;
                 let _lock = lock;
                 let _owner = owner;
+                // REVIEW ONLY: scheduling barrier; ownership is production code.
+                #[cfg(feature = "testing")]
+                if let Some(dir) = std::env::var_os("NET_A2A_REVIEW_OPEN_BARRIER") {
+                    let dir = std::path::PathBuf::from(dir);
+                    std::fs::write(dir.join("entered"), b"entered").map_err(|e| files.io_err(e))?;
+                    let start = std::time::Instant::now();
+                    while !dir.join("release").exists()
+                        && start.elapsed() < std::time::Duration::from_secs(10)
+                    {
+                        std::thread::sleep(std::time::Duration::from_millis(5));
+                    }
+                    let result = files.publish(&state);
+                    std::fs::write(dir.join("finished"), b"finished")
+                        .map_err(|e| files.io_err(e))?;
+                    return result;
+                }
                 files.publish(&state)
             })
             .await?;
